import Foundation

public extension CGFloat {

    // MARK: - Tokens declaration

    /**
    **`x1`** size constant describing the frame of an element and equal to **4 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_4pt: CGFloat = 4

    /**
    **`x10`** size constant describing the frame of an element and equal to **40 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_40pt: CGFloat = 40

    /**
    **`x112`** size constant describing the frame of an element and equal to **448 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_448pt: CGFloat = 448

    /**
    **`x12`** size constant describing the frame of an element and equal to **48 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_48pt: CGFloat = 48

    /**
    **`x128`** size constant describing the frame of an element and equal to **512 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_512pt: CGFloat = 512

    /**
    **`x16`** size constant describing the frame of an element and equal to **64 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_64pt: CGFloat = 64

    /**
    **`x160`** size constant describing the frame of an element and equal to **640 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_640pt: CGFloat = 640

    /**
    **`x18`** size constant describing the frame of an element and equal to **72 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_72pt: CGFloat = 72

    /**
    **`x192`** size constant describing the frame of an element and equal to **768 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_768pt: CGFloat = 768

    /**
    **`x2`** size constant describing the frame of an element and equal to **8 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_8pt: CGFloat = 8

    /**
    **`x24`** size constant describing the frame of an element and equal to **96 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_96pt: CGFloat = 96

    /**
    **`x3`** size constant describing the frame of an element and equal to **12 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_12pt: CGFloat = 12

    /**
    **`x32`** size constant describing the frame of an element and equal to **128 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_128pt: CGFloat = 128

    /**
    **`x36`** size constant describing the frame of an element and equal to **144 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_144pt: CGFloat = 144

    /**
    **`x4`** size constant describing the frame of an element and equal to **16 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_16pt: CGFloat = 16

    /**
    **`x48`** size constant describing the frame of an element and equal to **192 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_192pt: CGFloat = 192

    /**
    **`x5`** size constant describing the frame of an element and equal to **20 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_20pt: CGFloat = 20

    /**
    **`x6`** size constant describing the frame of an element and equal to **24 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_24pt: CGFloat = 24

    /**
    **`x64`** size constant describing the frame of an element and equal to **256 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_256pt: CGFloat = 256

    /**
    **`x8`** size constant describing the frame of an element and equal to **32 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_32pt: CGFloat = 32

    /**
    **`x96`** size constant describing the frame of an element and equal to **384 pt**.

    Can be used to define element `width`, `height`, etc... .
    */
    static let size_384pt: CGFloat = 384

}


// MARK: - all for SizeToken
public extension CGFloat {
    static let allSizeTokens: [CGFloat] = [.size_4pt, .size_40pt, .size_448pt, .size_48pt, .size_512pt, .size_64pt, .size_640pt, .size_72pt, .size_768pt, .size_8pt, .size_96pt, .size_12pt, .size_128pt, .size_144pt, .size_16pt, .size_192pt, .size_20pt, .size_24pt, .size_256pt, .size_32pt, .size_384pt]
}

// MARK: - Space assets declaration

public extension CGFloat {

    // MARK: - Tokens declaration

    /**
    **`x0`** spacing constant describing space between or around elements and equal to **0 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_0pt: CGFloat = 0

    /**
    **`x05`** spacing constant describing space between or around elements and equal to **2 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_2pt: CGFloat = 2

    /**
    **`x1`** spacing constant describing space between or around elements and equal to **4 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_4pt: CGFloat = 4

    /**
    **`x10`** spacing constant describing space between or around elements and equal to **40 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_40pt: CGFloat = 40

    /**
    **`x12`** spacing constant describing space between or around elements and equal to **48 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_48pt: CGFloat = 48

    /**
    **`x16`** spacing constant describing space between or around elements and equal to **64 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_64pt: CGFloat = 64

    /**
    **`x2`** spacing constant describing space between or around elements and equal to **8 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_8pt: CGFloat = 8

    /**
    **`x24`** spacing constant describing space between or around elements and equal to **96 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_96pt: CGFloat = 96

    /**
    **`x3`** spacing constant describing space between or around elements and equal to **12 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_12pt: CGFloat = 12

    /**
    **`x32`** spacing constant describing space between or around elements and equal to **128 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_128pt: CGFloat = 128

    /**
    **`x4`** spacing constant describing space between or around elements and equal to **16 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_16pt: CGFloat = 16

    /**
    **`x48`** spacing constant describing space between or around elements and equal to **192 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_192pt: CGFloat = 192

    /**
    **`x5`** spacing constant describing space between or around elements and equal to **20 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_20pt: CGFloat = 20

    /**
    **`x6`** spacing constant describing space between or around elements and equal to **24 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_24pt: CGFloat = 24

    /**
    **`x8`** spacing constant describing space between or around elements and equal to **32 pt**.

    Can be used for `padding`, ``HStack`` or ``VStack`` spacing, `offset`, ``Spacer`` size etc... .
    */
    static let space_32pt: CGFloat = 32

}


// MARK: - all for SpaceToken
public extension CGFloat {
    static let allSpaceTokens: [CGFloat] = [.space_0pt, .space_2pt, .space_4pt, .space_40pt, .space_48pt, .space_64pt, .space_8pt, .space_96pt, .space_12pt, .space_128pt, .space_16pt, .space_192pt, .space_20pt, .space_24pt, .space_32pt]
}

// MARK: - Radius assets declaration

public extension CGFloat {

    // MARK: - Tokens declaration

    /**
    Radius constant describing element corner radius and equal to **12 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_large: CGFloat = 12

    /**
    Radius constant describing element corner radius and equal to **8 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_medium: CGFloat = 8

    /**
    Radius constant describing element corner radius and equal to **4000 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_round: CGFloat = 4000

    /**
    Radius constant describing element corner radius and equal to **4 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_small: CGFloat = 4

    /**
    Radius constant describing element corner radius and equal to **16 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_xlarge: CGFloat = 16

    /**
    Radius constant describing element corner radius and equal to **2 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_xsmall: CGFloat = 2

    /**
    Radius constant describing element corner radius and equal to **20 pt**.

    Can be used for `cornerRadius`, `radius`, etc... .
    */
    static let radius_xxlarge: CGFloat = 20

}

public extension CGFloat {
    /// Returns negation value for your number (x*(-1)).
    /// Use to set nevative values for tokens
    func negation() -> CGFloat {
        self == .zero ? self : -self
    }
}


// MARK: - all for RadiusToken
public extension CGFloat {
    static let allRadiusTokens: [CGFloat] = [.radius_large, .radius_medium, .radius_round, .radius_small, .radius_xlarge, .radius_xsmall, .radius_xxlarge]
}
